<Window x:Class="ServiceGui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ServiceGui"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="400">
    <Grid>
        <TextBox Text="{Binding FileName}" Margin="50,36,128,353" />
        <TextBox BorderThickness="0"
                Text="Counter: " 
                 IsReadOnly="True" 
                 Margin="50,91,273,298"/>
        <TextBox Text="{Binding Counter}"
                 BorderThickness="0"
                 IsReadOnly="True"
                 Margin="119,91,229,298"/>
        <TextBox Text="{Binding Logs}"
                 Margin="50,154,54,47"
                 IsReadOnly="True"/>
    </Grid>
</Window>
